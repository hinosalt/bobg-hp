# Progress

Status: DONE
DoD: SATISFIED
Checks: PASS

## Current Sprint
- Next Task: ユーザーのローカル視認確認後の微調整対応
- Owner: agent
- Updated: 2026-02-18

## Known Blockers
- `registry.npmjs.org` が ENOTFOUND のため npm依存追加不可

## Notes
- 実装はオフライン完結の静的構成で提供。
- 不変性は `build_manifest` / `test:integrity` / `test:e2e` で検証可能。
- `上場実績` と `ニュース` を独立セクション化。
- `ニュース(3件)` のリンク先と画像を原本DOM由来URLで引用。
- `コアメンバー(5件)` / `アドバイザー(6件)` の写真を原本DOM由来URLで復元。
- `jp.zip/en.zip` を参照し、ENページに英語テキスト・ニュースリンク・主要画像差分を反映。
- `source/quoted/en/` にEN引用画像を配置し、ロケール別アセット読み分けを実装。
- `BOBG PTE. LTD. 読み込まれました` 表示は非表示化し、UI上から除去。
- `上場実績` ロゴは `object-fit: contain` へ変更し、見切れを解消。
- `資料請求` は固定フッターCTAとして常時表示（デスクトップ/モバイル対応）。
- `パートナー` 画像はカード全体を埋める表示に変更。
- ヘッダーの `EN / JA` 表示・切替リンクを JP/EN 両ページから完全削除。
- ヒーロー先頭の `トークン発行数 / トークン上場数` は画像表示を廃止し、同値(12/22)をUIカードで実装（JP/EN文言切替）。
- `協業プロジェクト` / `サービス` / `コアメンバー` の画像高さを圧縮し、視認性を維持したまま情報密度を最適化。
- `パートナー` ロゴは `object-fit: contain` に再調整し、見切れを防止。
- `hero metrics` を高度なビジュアルカードへ再設計し、配置をヒーローリード文直下へ移動（JP/EN共通）。
- ヒーロー画像を非表示化し、`hero metrics` を角張ったテクノロジーUI（グリッド/切り欠き/直線基調）へ再調整。
- `「」` 風の装飾を排し、`hero metrics` を角張りのシンプル設計へ整理。フォントウェイトも全体に軽くして可読性と誠実性を優先。
- Webフォントを `Noto Sans JP / Noto Sans` 構成へ変更し、過度な太字を抑制（見出し/ナビ/メトリクスを中心に調整）。
- `hero metrics` の枠を単線(1px)に統一し、初回ロード時のみ `0→規定値(12/22)` のカウントアップを実装。
- フッターの `資料請求はこちら` をホバー展開CTAへ変更（通常はコンパクト表示、ホバー/フォーカスでラベル展開。タッチ端末は常時表示）。
- フッター白帯を廃止し、`資料請求はこちら` のみを右下に浮遊表示。配色は原本準拠の黄地(`#FFFA00`)＋黒文字へ変更し、`description` は非表示化。
- `資料請求` ボタン押下で開く右寄せモーダル問い合わせ導線を追加（JP/EN文言切替、外側クリック/Escでクローズ）。
- ヒーロー画像DOM（`heroImage`）を JP/EN 両ページから削除し、メトリクス中心構成へ統一。
- ヘッダー右上に `EN / JA` 切替ボタンを復元し、JP→`en/` / EN→`../` の相互遷移を実装。
- モバイル表示を高密度化し、`1カラム巨大画像` を廃止。主要グリッドを 2〜3 カラム化、画像高さ/余白/文字サイズを圧縮して可読性を改善。
- ヘッダーナビが端末幅で2行化しないよう、`<=860px` で常時1行（横スクロール）に統一。
- ヘッダーをハンバーガーメニュー方式へ変更（`<=860px`）。`EN/JA` 切替は維持し、ナビは開閉パネルで表示。
- ヘッダーを `#17184B` 基調へ統一し、ロゴ枠を撤去。遷移メニューを同一ヘッダー行に統合し、可読性を保つ配色/フォーカスに再調整。
- 資料請求モーダル表示中は画面全体に半透明オーバーレイを付与し、背景を軽く減光表示。
- `問い合わせフォーム` / `資料請求モーダル` を必須入力化し、送信時に `formsubmit` 経由で `info@bobg.xyz` へ整形メール送信する実装を追加。
